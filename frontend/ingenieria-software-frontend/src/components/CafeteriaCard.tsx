import React from 'react'


type Cafeteria = {
    
    name: string; 
    location: string:
    imageURL: string;
    
}

const CafeteriaCard: React.FC<Cafeteria> = ({name, location, imageURL}) => {

    return (
	
	<div= className: 
	    <img= className:
	    <div= className:
	    <h2= className:
	    <p= className:

	

    );
};

